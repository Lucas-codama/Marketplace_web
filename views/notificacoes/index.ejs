<section>
  <div class="section-heading">
    <div>
      <span class="eyebrow">Atualizações</span>
      <h1>Notificações</h1>
    </div>
    <button type="button" id="marcarTodasLidas" class="btn btn-outline-dark">Marcar todas como lidas</button>
  </div>
  <p id="statusNotificacoes" class="visually-hidden" role="status" aria-live="polite" aria-atomic="true"></p>
  <div id="listaNotificacoes" class="d-grid gap-3">
    <% if (!notificacoes.length) { %>
      <div class="empty-state">Nenhuma notificação.</div>
    <% } %>
    <% notificacoes.forEach((item) => { %>
      <article class="notification-card <%= item.lida ? 'is-read' : 'is-unread' %>" data-notificacao-id="<%= item.id %>">
        <div>
          <h2 class="h5"><%= item.titulo %></h2>
          <span class="small fw-bold js-estado-notificacao"><%= item.lida ? 'Lida' : 'Não lida' %></span>
          <p class="text-secondary"><%= item.mensagem %></p>
          <small><%= new Date(item.createdAt).toLocaleString('pt-BR') %></small>
        </div>
        <div class="d-flex gap-2">
          <% if (item.link) { %>
            <a class="btn btn-sm btn-dark" href="<%= item.link %>">Abrir</a>
          <% } %>
          <% if (!item.lida) { %>
            <button type="button" class="btn btn-sm btn-link js-marcar-lida">Marcar <span class="visually-hidden"><%= item.titulo %> como </span>lida</button>
          <% } %>
        </div>
      </article>
    <% }) %>
  </div>
</section>
