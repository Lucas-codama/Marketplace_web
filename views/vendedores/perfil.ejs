<section><div class="seller-hero"><span class="eyebrow">Vendedor verificado</span><h1><%= perfil.nomeLoja %></h1><p><%= perfil.descricao || 'Loja parceira da NXT PLAY.' %></p><span><%= perfil.cidade || '' %><%= perfil.uf ? ` · ${perfil.uf}` : '' %></span></div><div class="section-heading mt-5"><h2>Produtos da loja</h2><span><%= produtos.length %> itens</span></div><div class="row g-3"><% produtos.forEach((produto) => { %><div class="col-6 col-lg-3"><%- include('../partials/produto-card', { produto }) %></div><% }) %></div></section>
